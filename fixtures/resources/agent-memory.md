# Agent Memory Fixture

This committed resource fixture verifies that an agent can ingest a real local
file through OpenViking resource ingestion.

Expected retrieval markers:

- Access phrase: opal-river-409
- Checksum color: indigo silver

If OpenViking indexed this file correctly, an agent should be able to search and
read the resource, then report both the access phrase and checksum color.
